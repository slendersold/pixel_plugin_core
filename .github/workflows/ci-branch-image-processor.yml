# Не-main ветки: только пакет image_processor.
name: CI branch · image_processor

on:
  push:
    branches-ignore:
      - main
      - master
    paths:
      - "image_processor/**"
      - ".github/workflows/ci-branch-image-processor.yml"

env:
  CARGO_TERM_COLOR: always

jobs:
  image_processor:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy

      - name: Install toolchains for C / Go plugins
        run: |
          sudo apt-get update
          sudo apt-get install -y build-essential golang-go

      - name: Format (workspace)
        run: cargo fmt --all -- --check

      - name: Clippy
        run: cargo clippy -p image_processor --all-targets --no-deps -- -D warnings

      - name: Build workspace (blur_plugin .so + CLI)
        run: cargo build --workspace

      - name: Tests
        run: cargo test -p image_processor
