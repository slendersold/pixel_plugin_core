[package]
name = "blur_plugin"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Плагин размытия (cdylib, FFI process_image)."

[lib]
crate-type = ["cdylib"]

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
