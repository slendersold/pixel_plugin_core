[package]
name = "mirror_plugin"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Плагин зеркального отражения (cdylib, FFI process_image)."

[lib]
crate-type = ["cdylib"]
